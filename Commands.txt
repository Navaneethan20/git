Start :
-----------------

git init
git status


Process : 
-----------------

git add .
git commit -m "some msg"
git push


Branching :
----------------- 

git branch <name>
git checkout <b_name> ---> to switch branch
git branch -a (all)
git branch -d <b_name> (dlt)
git checkout <b_name> ---> to switch branch